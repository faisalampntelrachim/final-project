render(){
    let elem =<div>{this.props.bio}<button>Edit</button></div>
    if(this.state.editing)
elem=
}else if (!this.props.bio){
    elem=
}
return elem;
}




render(){
    return(
        <div>
        {this.state.editting && <div>
            <textarea defaultValue
    )
}
